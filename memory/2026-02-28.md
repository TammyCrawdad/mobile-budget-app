# Memory — Saturday, February 28, 2026

## VibeBudgeting Full App Redesign

### Project
- Repo: loahou04/vibebudgeting (Tammy is collaborator with push access)
- Local clone: /home/fei/vibebudgeting
- Stack: React 19 + TypeScript + Tailwind CSS 4 + D3 + Vite + Express/MongoDB backend
- Deployed on Heroku (app name unknown — needs investigation)
- GitHub: TammyCrawdad account, credentials in 1Password op://TammyShareVault/GitHub/password

### Redesign — All merged to main (PR #19, squash merged, commit 495c94bf3b)

**Design system established:**
- Palette: indigo (#6366f1) → violet (#8b5cf6) gradient accents, slate text, #f1f5f9 background
- Font: Inter (Google Fonts)
- Cards: rounded-xl/2xl, 1.5px border, colored top-stripe, subtle shadow
- Buttons: gradient indigo→violet, 9px radius, box-shadow glow
- Inputs: bg-slate-50, border-slate-200, focus:border-indigo-500, rounded-xl, font-size:16px (prevents iOS zoom)
- Dark mode: fully supported, deep navy (#0f1729/#111827) card surfaces

**Components redesigned (10 files, 1170 insertions):**
1. Login.tsx — split-screen: dark navy left panel + white right form
2. Register.tsx — same split-screen with 3-step onboarding panel
3. Dashboard.tsx — gradient logo mark, indigo nav, redesigned summary cards, dynamic net-balance color
4. BudgetManager.tsx — transaction table polish + modal add/edit form
5. MobileBudgetView.tsx — mobile modal treatment
6. CategoryManager.tsx — card grid with color swatches + modal
7. BudgetForecast.tsx — summary cards + 3-column month grid
8. Charts.tsx — polished card containers
9. index.css — Inter font import
10. index.html — Google Fonts preconnect

**Key design decisions:**
- Net Balance card color = dynamic: green (vivid→muted) near zero → red (muted→vivid) based on savings rate
- Add/Edit transaction = true modal overlay, bottom-sheet on mobile (iPhone SE 375px min)
- Income/Expense = pill toggle (not dropdown)
- Recurring = iOS-style toggle switch (not checkbox)
- Modal pattern: fixed overlay + accent stripe + VB logo mark + scrollable body + sticky footer

### Heroku Deployment — BLOCKED on MFA
- Heroku credentials in 1Password: op://TammyShareVault/Heroku/
  - Email: hung.chen.ucf@gmail.com
  - Password: kvh4xym8rjc-PJY-pnh (account password, NOT API key)
- MFA is enabled on the account — can't use basic auth for API
- Need either:
  A) Heroku API token (Hung generates from account settings → API Key → Regenerate, add to 1P as "api_key")
  B) Browser relay handoff after Hung logs in with MFA
  C) GitHub auto-deploy if already configured in Heroku
- Heroku CLI: not installed on VPS (no sudo access)
- Deployment method: git push + heroku-postbuild (npm run build), start = cd backend && npm start

### Tooling Notes
- Screenshots: use `vite preview` or node SPA server (/tmp/spa-server.js) — NOT Python http.server (doesn't handle SPA routing)
- Playwright screenshots: use node script with chromium at /home/fei/.cache/ms-playwright/
- Codex agent: MUST use pty:true + background:true, no & in command, workdir=/home/fei/vibebudgeting
- Mockup server: python3 -m http.server 7788 in /home/fei/mockups (for static HTML mockups)
- SPA server: node /tmp/spa-server.js (port 7799, serves dist/)
- Mock API intercepts needed for dashboard screenshots (auth/validate, budget, categories, projections)

### Workflow Established with Hung
- Design proposal → screenshot mockup → send before/after → wait for approval → build with codex → screenshot real result → push → repeat
- Hung reviews each component before building
- Same branch accumulates commits, PR at end
